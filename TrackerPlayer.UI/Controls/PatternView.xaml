<UserControl x:Class="TrackerPlayer.UI.Controls.PatternView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="#0D0F14"
             ClipToBounds="True">
    <Grid>
        <!-- ScrollViewer : scroll horizontal uniquement, pas vertical -->
        <ScrollViewer x:Name="PatternScroll"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled"
                      Focusable="False"
                      ScrollChanged="PatternScroll_ScrollChanged">
            <Canvas x:Name="PatternCanvas" ClipToBounds="True"/>
        </ScrollViewer>

        <!--
            Header fixe superposé PAR-DESSUS le ScrollViewer.
            Placé après dans le XAML = z-order supérieur.
            Clip horizontal géré par code (translate transform).
            IsHitTestVisible=False pour ne pas bloquer les interactions.
        -->
        <Canvas x:Name="HeaderCanvas"
                VerticalAlignment="Top"
                HorizontalAlignment="Left"
                IsHitTestVisible="False"
                ClipToBounds="True"/>
    </Grid>
</UserControl>
