<Window x:Class="DemoBase.App.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="700" Height="700"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid>
        <!-- Fond avec ombre -->
        <Border Background="#FF000000" CornerRadius="18" Margin="20">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="0" Opacity="0.9"/>
            </Border.Effect>
        </Border>

        <!-- Double trait extérieur -->
        <Border BorderBrush="#FF666666" BorderThickness="2" CornerRadius="16" Margin="20"/>

        <!-- Trait intérieur -->
        <Border BorderBrush="#FF444444" BorderThickness="1" CornerRadius="13" Margin="27"/>

        <!-- Image par-dessus tout -->
        <Image x:Name="SplashImage"
               Margin="30"
               Stretch="Uniform"
               RenderOptions.BitmapScalingMode="HighQuality"/>
    </Grid>
</Window>
