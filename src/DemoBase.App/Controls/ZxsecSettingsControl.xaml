<UserControl x:Class="DemoBase.App.Views.Emulators.ZxsecSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource BgCard}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusLg}"
            Padding="18,14" Margin="0,0,0,20">
        <StackPanel>

            <!-- Titre -->
            <DockPanel Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Right"
                           Text="ZXSEC" FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}"
                           VerticalAlignment="Center"/>
                <TextBlock Text="{DynamicResource Emu_ZxsecConfig}" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
            </DockPanel>

            <!-- Modèle -->
            <TextBlock Text="{DynamicResource Emu_MachineType}" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
            <ComboBox Style="{StaticResource FlatComboBox}" Margin="0,0,0,12"
                      SelectedValue="{Binding MachineModel}"
                      SelectedValuePath="Tag">
                <ComboBoxItem Content="Sinclair Spectrum 48K"  Tag="0"/>
                <ComboBoxItem Content="Sinclair Spectrum 128K" Tag="1"/>
                <ComboBoxItem Content="Sinclair Spectrum +2"   Tag="2"/>
                <ComboBoxItem Content="Sinclair Spectrum +3"   Tag="3"/>
            </ComboBox>

            <!-- Affichage -->
            <TextBlock Text="{DynamicResource Emu_Display}" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <CheckBox Content="{DynamicResource Emu_CpcecFullscreen}" IsChecked="{Binding FullScreen}" Margin="0,0,16,0"/>
                <CheckBox Content="{DynamicResource Emu_CpcecIndicators}" IsChecked="{Binding ShowIndicators}"/>
            </StackPanel>

        </StackPanel>
    </Border>
</UserControl>
