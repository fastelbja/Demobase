<UserControl x:Class="DemoBase.App.Views.Emulators.PPSSPPSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource BgCard}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusLg}"
            Padding="18,14" Margin="0,0,0,20">
        <StackPanel>

            <DockPanel Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Right"
                           Text="PPSSPP" FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
                <TextBlock Text="PlayStation Portable" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
            </DockPanel>

            <!-- Affichage -->
            <TextBlock Text="{DynamicResource Emu_Display}" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
            <CheckBox Content="{DynamicResource Emu_CpcecFullscreen}"
                      IsChecked="{Binding FullScreen}" Margin="0,0,0,12"/>

            <!-- Options frontend -->
            <TextBlock Text="Options frontend" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
            <CheckBox IsChecked="{Binding EscapeExit}" Margin="0,0,0,6">
                <CheckBox.Content>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="--escape-exit"/>
                        <Run Text=" — ESC ferme PPSSPP immédiatement"
                             Foreground="{DynamicResource TextSecondary}" FontSize="10"/>
                    </TextBlock>
                </CheckBox.Content>
            </CheckBox>
            <CheckBox IsChecked="{Binding PauseMenuExit}" Margin="0,0,0,4">
                <CheckBox.Content>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="--pause-menu-exit"/>
                        <Run Text=" — menu pause : 'Exit to menu' devient 'Exit'"
                             Foreground="{DynamicResource TextSecondary}" FontSize="10"/>
                    </TextBlock>
                </CheckBox.Content>
            </CheckBox>

        </StackPanel>
    </Border>
</UserControl>
