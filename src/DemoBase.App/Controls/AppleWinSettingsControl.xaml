<UserControl x:Class="DemoBase.App.Views.Emulators.AppleWinSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DemoBase.App.ViewModels.Emulators">
    <Border Background="{DynamicResource BgCard}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusLg}"
            Padding="18,14" Margin="0,0,0,20">
        <StackPanel>

            <DockPanel Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Right"
                           Text="AppleWin" FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
                <TextBlock Text="Apple II / IIe" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
            </DockPanel>

            <!-- Modèle -->
            <TextBlock Text="Modèle de machine" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
            <ComboBox Style="{StaticResource FlatComboBox}" Margin="0,0,0,12"
                      ItemsSource="{x:Static vm:AppleWinSettingsViewModel.ModelOptions}"
                      DisplayMemberPath="Key"
                      SelectedValue="{Binding Model}"
                      SelectedValuePath="Value"/>

            <!-- Fréquence -->
            <TextBlock Text="Fréquence vidéo" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
            <ComboBox Style="{StaticResource FlatComboBox}" Margin="0,0,0,12"
                      ItemsSource="{x:Static vm:AppleWinSettingsViewModel.FreqOptions}"
                      DisplayMemberPath="Key"
                      SelectedValue="{Binding Freq}"
                      SelectedValuePath="Value"/>

            <!-- Power-On -->
            <CheckBox IsChecked="{Binding PowerOn}" Margin="0,0,0,12">
                <CheckBox.Content>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="-power-on"/>
                        <Run Text=" — allume automatiquement (sans appuyer sur une touche)"
                             Foreground="{DynamicResource TextSecondary}" FontSize="10"/>
                    </TextBlock>
                </CheckBox.Content>
            </CheckBox>

            <!-- Note -->
            <Border Background="{DynamicResource BgSurface}" CornerRadius="4" Padding="10,8">
                <TextBlock FontSize="10" Foreground="{DynamicResource TextSecondary}"
                           TextWrapping="Wrap"
                           Text="Fullscreen : pas d'argument CLI — configurer dans le menu AppleWin (Video). Disque côté B : ajouter -d2 &quot;side2.dsk&quot; dans le champ Command Line du profil. AppleWin ne supporte pas l'Apple IIgs — utiliser GSplus."/>
            </Border>

        </StackPanel>
    </Border>
</UserControl>
