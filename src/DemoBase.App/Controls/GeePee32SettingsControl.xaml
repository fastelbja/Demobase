<UserControl x:Class="DemoBase.App.Views.Emulators.GeePee32SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource BgCard}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusLg}"
            Padding="18,14" Margin="0,0,0,20">
        <StackPanel>

            <DockPanel Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Right"
                           Text="gp32emu / GeePee32" FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
                <TextBlock Text="GamePark GP32" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
            </DockPanel>

            <!-- Options -->
            <TextBlock Text="Options" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
            <CheckBox Content="Passer l'écran de démarrage (/nosplash)"
                      IsChecked="{Binding NoSplash}" Margin="0,0,0,12"/>

            <!-- Info BIOS / formats -->
            <Border Background="{DynamicResource BgSurface}" CornerRadius="4" Padding="10,8">
                <StackPanel>
                    <TextBlock FontSize="10" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextSecondary}"
                               Text="Formats &amp; BIOS" Margin="0,0,0,4"/>
                    <TextBlock FontSize="10" Foreground="{DynamicResource TextSecondary}"
                               TextWrapping="Wrap"
                               Text="gp32emu (recommandé) : passer le fichier directement, BIOS optionnel (HLE intégré). GeePee32 legacy : BIOS firmware requis dans geepee32.ini. Formats : .fxe (démos/homebrew) .gxb (commercial) .smc (SmartMedia, passé auto avec /SMC= pour GeePee32)."/>
                </StackPanel>
            </Border>

        </StackPanel>
    </Border>
</UserControl>
