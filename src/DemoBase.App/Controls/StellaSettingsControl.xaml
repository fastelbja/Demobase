<UserControl x:Class="DemoBase.App.Views.Emulators.StellaSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DemoBase.App.ViewModels.Emulators">
    <Border Background="{DynamicResource BgCard}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusLg}"
            Padding="18,14" Margin="0,0,0,20">
        <StackPanel>

            <DockPanel Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Right"
                           Text="Stella" FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
                <TextBlock Text="Atari 2600 VCS" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
            </DockPanel>

            <!-- Affichage -->
            <TextBlock Text="{DynamicResource Emu_Display}" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
            <CheckBox Content="{DynamicResource Emu_CpcecFullscreen}"
                      IsChecked="{Binding FullScreen}" Margin="0,0,0,6"/>
            <CheckBox Content="VSync (-vsync 1)"
                      IsChecked="{Binding VSync}" Margin="0,0,0,12"/>

            <!-- Zoom -->
            <TextBlock Text="Zoom (mode fenêtré)" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
            <ComboBox Style="{StaticResource FlatComboBox}" Margin="0,0,0,12"
                      ItemsSource="{x:Static vm:StellaSettingsViewModel.ZoomOptions}"
                      DisplayMemberPath="Key"
                      SelectedValue="{Binding Zoom}"
                      SelectedValuePath="Value"/>

            <!-- Palette -->
            <TextBlock Text="Palette de couleurs" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
            <ComboBox Style="{StaticResource FlatComboBox}" Margin="0,0,0,4"
                      ItemsSource="{x:Static vm:StellaSettingsViewModel.PaletteOptions}"
                      DisplayMemberPath="Key"
                      SelectedValue="{Binding Palette}"
                      SelectedValuePath="Value"/>

        </StackPanel>
    </Border>
</UserControl>
