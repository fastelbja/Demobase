<UserControl x:Class="DemoBase.App.Views.Emulators.JavaSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource BgCard}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusLg}"
            Padding="18,14" Margin="0,0,0,20">
        <StackPanel>

            <DockPanel Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Right"
                           Text="Java" FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
                <TextBlock Text="Configuration JVM" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
            </DockPanel>

            <!-- JVM Args -->
            <TextBlock Text="Arguments JVM" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
            <TextBox Style="{StaticResource SearchBox}"
                     Padding="8,6" FontFamily="Consolas" FontSize="12"
                     Text="{Binding JvmArgs, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,4"/>
            <TextBlock FontSize="10" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"
                       TextWrapping="Wrap"
                       Text="-Xmx512m = 512 Mo max   -Xms128m = 128 Mo initial   -Dsun.java2d.opengl=true = accél. OpenGL"/>

            <!-- Note javaw.exe -->
            <Border Background="{DynamicResource BgSurface}"
                    CornerRadius="4" Padding="10,8">
                <TextBlock FontSize="10" Foreground="{DynamicResource TextSecondary}"
                           TextWrapping="Wrap"
                           Text="L'exécutable de l'émulateur doit pointer vers javaw.exe (sans fenêtre console). Si le champ est vide, DemoBase tente 'javaw' depuis le PATH système. La release doit contenir un fichier .jar."/>
            </Border>

        </StackPanel>
    </Border>
</UserControl>
