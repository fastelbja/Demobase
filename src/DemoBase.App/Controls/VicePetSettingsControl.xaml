<UserControl x:Class="DemoBase.App.Views.Emulators.VicePetSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource BgCard}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusLg}"
            Padding="18,14" Margin="0,0,0,20">
        <StackPanel>

            <!-- Titre -->
            <DockPanel Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Right"
                           Text="VICE" FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}"
                           VerticalAlignment="Center"/>
                <TextBlock Text="{DynamicResource Emu_VicePetConfig}" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
            </DockPanel>

            <!-- Modèle -->
            <TextBlock Text="{DynamicResource Emu_MachineType}" Style="{StaticResource FieldLabel}" Margin="0,0,0,4"/>
            <ComboBox Style="{StaticResource FlatComboBox}" Margin="0,0,0,12"
                      SelectedValue="{Binding Model}"
                      SelectedValuePath="Tag">
                <ComboBoxItem Content="3032 (40 col., petit écran)"  Tag="3032"/>
                <ComboBoxItem Content="4032 (40 col.)"                Tag="4032"/>
                <ComboBoxItem Content="8032 (80 col., défaut)"        Tag="8032"/>
                <ComboBoxItem Content="8096 (80 col., 96 Kio)"        Tag="8096"/>
                <ComboBoxItem Content="8296 (80 col., 128 Kio)"       Tag="8296"/>
                <ComboBoxItem Content="SuperPET (9000, 6809)"         Tag="superpet"/>
            </ComboBox>

            <!-- Lecteur -->
            <CheckBox Content="{DynamicResource Emu_TrueDrive}" IsChecked="{Binding TrueDrive}" Margin="0,0,0,4"/>
            <TextBlock Text="{DynamicResource Emu_TrueDriveHint}" FontSize="11"
                       Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12"
                       TextWrapping="Wrap"/>

            <!-- Affichage -->
            <TextBlock Text="{DynamicResource Emu_Display}" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
            <CheckBox Content="{DynamicResource Emu_PetFullscreen}" IsChecked="{Binding FullScreen}"/>

        </StackPanel>
    </Border>
</UserControl>
