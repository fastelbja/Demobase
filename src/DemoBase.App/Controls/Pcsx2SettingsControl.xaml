<UserControl x:Class="DemoBase.App.Views.Emulators.Pcsx2SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{DynamicResource BgCard}"
            BorderBrush="{DynamicResource Border}" BorderThickness="1"
            CornerRadius="{StaticResource RadiusLg}"
            Padding="18,14" Margin="0,0,0,20">
        <StackPanel>

            <DockPanel Margin="0,0,0,14">
                <TextBlock DockPanel.Dock="Right"
                           Text="PCSX2" FontSize="10" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
                <TextBlock Text="PlayStation 2" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimary}"/>
            </DockPanel>

            <!-- Affichage -->
            <TextBlock Text="{DynamicResource Emu_Display}" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>
            <CheckBox Content="{DynamicResource Emu_CpcecFullscreen}"
                      IsChecked="{Binding FullScreen}" Margin="0,0,0,12"/>

            <!-- Options frontend -->
            <TextBlock Text="Options frontend" Style="{StaticResource FieldLabel}" Margin="0,0,0,8"/>

            <CheckBox IsChecked="{Binding Batch}" Margin="0,0,0,6">
                <CheckBox.Content>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="-batch"/>
                        <Run Text=" — quitte PCSX2 automatiquement à l'arrêt du jeu"
                             Foreground="{DynamicResource TextSecondary}" FontSize="10"/>
                    </TextBlock>
                </CheckBox.Content>
            </CheckBox>

            <CheckBox IsChecked="{Binding FastBoot}" Margin="0,0,0,6">
                <CheckBox.Content>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="-fastboot"/>
                        <Run Text=" — passe l'animation BIOS Sony au démarrage"
                             Foreground="{DynamicResource TextSecondary}" FontSize="10"/>
                    </TextBlock>
                </CheckBox.Content>
            </CheckBox>

            <CheckBox IsChecked="{Binding NoGui}" Margin="0,0,0,4">
                <CheckBox.Content>
                    <TextBlock TextWrapping="Wrap">
                        <Run Text="-nogui"/>
                        <Run Text=" — masque la fenêtre principale Qt (jeu direct, implique -batch)"
                             Foreground="{DynamicResource TextSecondary}" FontSize="10"/>
                    </TextBlock>
                </CheckBox.Content>
            </CheckBox>

            <!-- Sauvegarde -->
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                <Button Content="{DynamicResource Btn_Save}"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource AccentButton}"
                        Padding="16,7"/>
                <TextBlock Text="{Binding SaveMessage}"
                           Foreground="{DynamicResource TextSecondary}"
                           FontSize="11" VerticalAlignment="Center"
                           Margin="12,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
