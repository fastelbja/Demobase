<UserControl x:Class="DemoBase.App.Views.WizardPages.EmulatorsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:DemoBase.App.Views">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="{DynamicResource Wiz_EmuTitle}" FontSize="28" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimary}"
                       Margin="0,0,0,6"/>
            <TextBlock FontSize="13" Foreground="{DynamicResource TextSecondary}"
                       TextWrapping="Wrap"
                       Text="{DynamicResource Wiz_EmuSubtitle}"/>
        </StackPanel>

        <!-- Vue d'installation existante, réutilisée telle quelle -->
        <Border Grid.Row="1"
                BorderBrush="{DynamicResource Border}"
                BorderThickness="1" CornerRadius="6"
                ClipToBounds="True">
            <views:EmulatorInstallerView x:Name="InstallerView"/>
        </Border>

    </Grid>
</UserControl>
